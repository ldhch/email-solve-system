# Codex 交付提示词

> 用法：复制下方**首次提示词**整段粘贴给 Codex。完成 Phase 1 后，用「阶段推进提示词」进入下一阶段。每次新会话都以「阶段推进提示词」开场（AGENTS.md 会自动加载项目约束）。

---

## 首次提示词（Phase 1）

```
你是资深 Python 全栈工程师，为本项目实现代码。

【第一步】先完整读取以下三个文件，全部读完再动手：
1. ./AGENTS.md —— 项目规范与约束（红线，必须严格遵守，禁止引入清单外的任何技术）
2. ./PRD.md —— 业务需求
3. ./TECH.md —— 技术方案（含模块拆分、数据库、接口契约、开发顺序、**第七章目录结构**）

【任务】
按 TECH.md 第九章的开发顺序，只实现【Phase 1（里程碑 A）】：
跑通「IMAP 拉邮件 → 会话合并 → LLM 分类 → 低风险直接自动回复」完整链路，
并具备紧急暂停开关（F9）与审计日志（F10 最小版）。
不要实现 Phase 2 及以后的内容。

【硬性要求】
- 严格遵守 AGENTS.md 第三节的简化约束，禁止引入 RAG/向量库/任务队列/Alembic/CSRF/Agent 框架等
- 项目骨架严格按 TECH.md 第七章目录结构创建，模块文件路径与文档保持一致（如 backend/app/services/ingest.py、backend/app/llm/client.py），不得自行另起目录结构；Phase 1 只建本阶段需要的文件，不预建 Phase 2+ 的空壳
- 代码与注释用英文；对客界面文案英文；后台界面文案中文
- 所有密钥与配置走 .env（提供 .env.example），禁止硬编码；禁止写入真实密码
- 数据库用 SQLite（WAL），create_all + seed 脚本，不用迁移框架
- LLM 用 DeepSeek（deepseek-v4-flash），封装在 llm/client.py，预留 provider 替换口

【完成标准（缺一不可）】
1. pytest 测试通过，并给出实际运行输出
2. 给出本地手动验证步骤：如何启动、如何发一封测试邮件走完整流程看到低风险自动回复
3. 更新 README.md（本地启动 + .env 配置说明）
4. 写 IMPLEMENTATION_NOTES.md：完成项 / 未决项 / 待我确认的问题（一次性列清）
5. 明确声明：本阶段完成、未做下一阶段

【交付前自检清单】
- [ ] .env.example 覆盖所有配置项
- [ ] 测试运行通过并有输出
- [ ] README 写清启动步骤
- [ ] 未引入 Phase 2 的功能
```

---

## 阶段推进提示词（Phase 2 / 3 / 4）

```
继续本项目开发。按 AGENTS.md 规范，基于已完成的 Phase 1（代码 + IMPLEMENTATION_NOTES.md），
实现 TECH.md 第九章的【Phase N】：<这里写该 Phase 的标题，如"后台串联 + 挽留（里程碑 B）">。
其余硬性要求与完成标准同 Phase 1（测试通过、README 更新、IMPLEMENTATION_NOTES.md 记录、不越界到下一 Phase）。
先读 IMPLEMENTATION_NOTES.md 了解当前状态与待确认问题，若其中有阻塞本阶段的问题，先集中列出等我确认。
```

> 推进时把"Phase N"替换为对应标题：Phase 2 后台串联 + 挽留（里程碑 B）；Phase 3 高风险 & 知识库 & 标准QA（里程碑 C）；Phase 4 监控 & 安全 & 部署（里程碑 D）。

---

## 使用要点

1. **一次只推进一个 Phase**：Codex 做完一个 Phase 会停下等你确认，看到 IMPLEMENTATION_NOTES.md 的"待确认问题"后集中回复，不要零星拉扯。
2. **每个 Phase 让 Codex 先跑测试再交**：它必须给出运行输出，而不是口头说"完成了"。
3. **遇到它提的疑问**：拿不准就让它按「宁可转人工，不让 AI 乱发」处理，业务细节你会改主意就直说。
4. **代码出了岔子**：git 已初始化，让它用 git 回滚或对比，不要重来。

---

## Claude 代码审核提示词

> 每个 Phase 完成后，把下方提示词复制给 Claude Code 做一次 review。审核结论决定能否进入下一阶段。
> 建议力度：**Phase 1 / 4 重点审**（架构 + 安全）；**Phase 2 / 3 轻量审**（重点盯业务规则）。

```
你是资深代码审查者，对 Codex 刚完成的代码做一次严格 review。
只审查，不修改代码、不修改文档。

【背景】先完整读取：
1. ./AGENTS.md —— 项目红线与规范
2. ./PRD.md —— 业务需求
3. ./TECH.md —— 技术方案
4. ./IMPLEMENTATION_NOTES.md —— Codex 的交付记录（本次审核的对象与范围）
5. 项目当前全部代码（backend/ 与 frontend/）

【审核目标】（按优先级）
1. 对照 TECH.md：本阶段该做的模块是否完整实现，有无遗漏或留空
2. 是否违反 AGENTS.md 红线：是否引入了被禁止的技术（RAG/向量库/任务队列/Alembic/CSRF/Agent 框架等），有无"自作主张加东西"
3. 业务规则正确性：对照 PRD.md，本阶段相关业务行为是否实现正确（如挽留轮次、拒付拦截、审核流）
   其中【多轮记忆/会话合并（PRD F2）】为必查项：客户第二次发邮件时，能否正确归入同一会话（In-Reply-To 优先）、历史是否持久化存储并注入回复生成、同一会话风险是否取最高、是否聚合回复避免刷屏
4. 安全红线：是否硬编码密钥/密码/接口地址；.env.example 是否完整；敏感信息是否泄漏
5. 代码质量：是否有明显 bug、死代码、半成品、未实现却已提交的功能

【输出格式】
按严重程度分级：
- 🔴 阻塞：必须修复才能进入下一阶段（bug、红线违规、业务规则错误）
- 🟡 建议：不阻塞但应该修（质量/可维护性问题）
- 🔵 提示：可选优化
每条给：文件路径:行号 + 问题描述 + 建议修复方向
最后给一句结论：本阶段代码【可以/不可以】进入下一阶段，并说明原因。
```
